workflow: GitFlow/v1
mode: ContinuousDeployment

next-version: 1.1.0
branches:
  develop:
    regex: ^develop
    
  release:
    regex: ^release
    is-release-branch: false
    
  main:
    regex: ^main
    is-release-branch: true
    is-mainline: true
